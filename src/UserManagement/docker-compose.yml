version: '3.4'

services:
  usermanagement.api:
    image: ${DOCKER_REGISTRY-}usermanagementapi
    build:
      context: .
      dockerfile: Modules/UserManagement.Api/Dockerfile

  authorization.api:
    image: ${DOCKER_REGISTRY-}authorizationapi
    build:
      context: .
      dockerfile: Modules/Authorization.Api/Dockerfile

  web.ui:
    image: ${DOCKER_REGISTRY-}webui
    build:
      context: user-management-web-ui
      dockerfile: Dockerfile

